const utils = {
    checkBook(book){
        for(var item in book){
            switch (item) {
                case value:
                    
                    break;
            
                default:
                    break;
            }
        }
    }
}

export default utils;